/** @type {import('next').NextConfig} */
const nextConfig = {
    // Rimuovi output: 'export' se lo avevi
    // Non serve più specificare nulla di speciale per Netlify
  }
  
  module.exports = nextConfig